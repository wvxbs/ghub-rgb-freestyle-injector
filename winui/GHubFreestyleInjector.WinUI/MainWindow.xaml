<Window
    x:Class="GHubFreestyleInjector.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="G HUB RGB Freestyle Injector">

    <Grid>
        <NavigationView
            x:Name="RootNavigation"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            PaneDisplayMode="LeftCompact"
            Header="Sincronizar presets"
            SelectionChanged="RootNavigation_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Sincronizar" Icon="Sync" Tag="sync" IsSelected="True" />
                <NavigationViewItem Content="Logs" Icon="Document" Tag="logs" />
                <NavigationViewItem Content="Sobre" Icon="Help" Tag="about" />
            </NavigationView.MenuItems>

            <Grid Padding="28" RowSpacing="18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <InfoBar
                    x:Name="StatusBar"
                    IsOpen="True"
                    Severity="Informational"
                    Title="Pronto"
                    Message="Escolha a pasta de paletas e execute uma simulação antes de aplicar." />

                <Grid Grid.Row="1">
                    <Grid x:Name="SyncPage" RowSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid RowSpacing="12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="Entradas" Style="{StaticResource SubtitleTextBlockStyle}" />

                                <Grid Grid.Row="1" ColumnSpacing="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox
                                        x:Name="InputPathBox"
                                        Header="Pasta das paletas"
                                        PlaceholderText="C:\Caminho\Para\RGB-palettes" />
                                    <Button
                                        Grid.Column="1"
                                        Margin="0,28,0,0"
                                        Content="Escolher"
                                        Click="ChooseInput_Click" />
                                </Grid>

                                <Grid Grid.Row="2" ColumnSpacing="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox
                                        x:Name="DbPathBox"
                                        Header="settings.db do G HUB"
                                        PlaceholderText="C:\Users\...\AppData\Local\LGHUB\settings.db" />
                                    <Button
                                        Grid.Column="1"
                                        Margin="0,28,0,0"
                                        Content="Escolher"
                                        Click="ChooseDb_Click" />
                                </Grid>
                            </Grid>
                        </Border>

                        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
                            <StackPanel Spacing="12">
                                <TextBlock Text="Opções" Style="{StaticResource SubtitleTextBlockStyle}" />
                                <CheckBox x:Name="KillGHubBox" Content="Encerrar G HUB antes de aplicar" IsChecked="True" />
                                <CheckBox x:Name="ForceBox" Content="Forçar regravação dos presets detectados" />
                                <CheckBox x:Name="PruneBox" Content="Remover presets RGB órfãos" />
                            </StackPanel>
                        </Border>

                        <CommandBar Grid.Row="2" DefaultLabelPosition="Right" Background="Transparent">
                            <AppBarButton Icon="List" Label="Listar" Click="List_Click" />
                            <AppBarButton Icon="Preview" Label="Simular" Click="DryRun_Click" />
                            <AppBarButton Icon="Accept" Label="Aplicar" Click="Apply_Click" />
                            <AppBarSeparator />
                            <AppBarButton Icon="Cancel" Label="Encerrar G HUB" Click="KillGHub_Click" />
                        </CommandBar>

                        <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}">
                            <Grid RowSpacing="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="Saída" Style="{StaticResource SubtitleTextBlockStyle}" />
                                <TextBox
                                    x:Name="OutputBox"
                                    Grid.Row="1"
                                    AcceptsReturn="True"
                                    IsReadOnly="True"
                                    TextWrapping="NoWrap"
                                    FontFamily="Cascadia Mono, Consolas"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    MinHeight="220" />
                            </Grid>
                        </Border>
                    </Grid>

                    <Grid x:Name="LogsPage" Visibility="Collapsed">
                        <Border Style="{StaticResource CardBorderStyle}">
                            <TextBox
                                x:Name="FullLogBox"
                                AcceptsReturn="True"
                                IsReadOnly="True"
                                TextWrapping="NoWrap"
                                FontFamily="Cascadia Mono, Consolas"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                ScrollViewer.HorizontalScrollBarVisibility="Auto" />
                        </Border>
                    </Grid>

                    <Grid x:Name="AboutPage" Visibility="Collapsed">
                        <Border Style="{StaticResource CardBorderStyle}" MaxWidth="720" HorizontalAlignment="Left" VerticalAlignment="Top">
                            <StackPanel Spacing="12">
                                <TextBlock Text="G HUB RGB Freestyle Injector" Style="{StaticResource TitleTextBlockStyle}" />
                                <TextBlock Text="Shell experimental em WinUI 3 para a CLI ghub-freestyle." TextWrapping="Wrap" />
                                <TextBlock Text="Esta interface usa tema automático do Windows, materiais do Windows App SDK quando disponíveis e mantém o motor Python/CLI separado." TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </NavigationView>
    </Grid>
</Window>
